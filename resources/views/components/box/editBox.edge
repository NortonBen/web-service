
@component('components.box.box')

    @slot('title')
        Sửa {{ name }}
    @endslot

    @slot('action')
        <button onClick="document.forms['form-edit-{{ prefix }}'].submit()" class="btn btn-success form-action">Lưu Sửa</button>
        <a href="{{ actionBack }}" class="btn btn-danger form-action">Trở Lại</a>
    @endslot

    <div class="col-md-10 col-md-offset-1">
        <form id="form-edit-{{ prefix }}" action="{{ action }}" method="post" {{ extend }}>
            @!yield($slot.main)
            {{ csrf }}
            <div class="col-md-12" style="text-align: center;">
                <button type="submit" class="btn btn-success form-action">Lưu Sửa</button>
                <a href="{{ actionBack }}" class="btn btn-danger form-action">Trở Lại</a>
            </div>
        </from>
    </div>
   
@endcomponent

