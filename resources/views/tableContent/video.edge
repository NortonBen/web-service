<tr>
    <td class="col-md-2" >{{ item.id }}</td>
    <td>{{ item.name }}</td>
    <td class="col-md-2" >{{ item.state.name }}</td>
    <td class="col-md-2" >{{ item.created_at }}</td>
    <td class="col-md-2">
        <a class="btn btn-info" href="{{ route('admin.videos.edit', { id : item.id }) }}" >Sửa</a>
        <a class="btn btn-danger" onClick="document.getElementById('action-delete-post-{{ item.id }}').submit()" href="#" >Xóa</a>
        <form id="action-delete-post-{{ item.id }}" class='delete' action="{{ route('admin.videos.destroy', { id : item.id }) }}?_method=DELETE" method="post">
            <input name="_csrf" type="hidden" value="">   
        </form>
    </td>
</tr>